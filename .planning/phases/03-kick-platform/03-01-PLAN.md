---
phase: 03-kick-platform
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test-kick-detection.spec.ts
  - tests/test-kick-injection.spec.ts
  - tests/test-postmessage-origin.spec.ts
  - tests/test-kick-spa-navigation.spec.ts
  - tests/test-kick-selector-fallback.spec.ts
  - tests/fixtures/kick-mock.html
autonomous: true
requirements:
  - KICK-01
  - KICK-02
  - KICK-05
  - KICK-06
  - KICK-07

must_haves:
  truths:
    - "Six new test files exist: five spec stubs and one fixture HTML"
    - "All five spec files skip their tests (no red failures in CI before implementation)"
    - "kick-mock.html renders a fake Kick stream page with #channel-chatroom slot present"
    - "npx playwright test --project=chromium-extension exits green (all stubs skip, none error)"
  artifacts:
    - path: "tests/test-kick-detection.spec.ts"
      provides: "skip-stubs for KICK-01 live stream detection tests"
    - path: "tests/test-kick-injection.spec.ts"
      provides: "skip-stubs for KICK-02 slot injection tests"
    - path: "tests/test-postmessage-origin.spec.ts"
      provides: "skip-stubs for KICK-05 postMessage origin validation (Twitch + YouTube regression + Kick)"
    - path: "tests/test-kick-spa-navigation.spec.ts"
      provides: "skip-stubs for KICK-06 SPA navigation tests"
    - path: "tests/test-kick-selector-fallback.spec.ts"
      provides: "skip-stubs for KICK-07 selector fallback chain tests"
    - path: "tests/fixtures/kick-mock.html"
      provides: "Kick platform fixture HTML with #channel-chatroom slot"
  key_links:
    - from: "tests/test-kick-injection.spec.ts"
      to: "tests/fixtures/kick-mock.html"
      via: "page.goto file path reference"
      pattern: "kick-mock\\.html"
---

<objective>
Create Wave 0 test scaffolding for Phase 3: five spec files with skip-stubs covering all KICK requirements, plus the kick-mock.html fixture HTML needed for offline injection tests.

Purpose: Establishes the Nyquist-compliant test infrastructure before any implementation code exists. Executors in subsequent plans reference these test files in their `<verify>` commands. Skipped stubs keep CI green while the implementation is being built.
Output: 6 files — 5 test specs + 1 fixture HTML.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-kick-platform/03-CONTEXT.md
@.planning/phases/03-kick-platform/03-VALIDATION.md

<interfaces>
<!-- Established skip-stub pattern from Phase 2 Wave 0 (test-design-system.spec.ts): -->

Sync tests (no page/browser needed):
```typescript
test.skip('KICK-XX: description of behavior', () => {
  // Implementation pending
});
```

Async fixture tests (need page object):
```typescript
test('KICK-XX: description of behavior', async ({ page }) => {
  test.skip();
  // Requires built extension + fixture page
  // Implementation pending
});
```

Use `test.skip()` (runtime) for tests that need `page` fixture — preserves async signature.
Use `test.skip(...)` (static) for pure fs/unit tests — no async needed.

Kick fixture selectors to include in kick-mock.html:
- Primary chat slot: `#channel-chatroom`
- Fallback 1: `#chatroom`
- Fallback 2: `.chatroom-wrapper`
- Live badge: `[data-state="live"]` (placeholder — confirmed at KICK-07 checkpoint)

Existing fixture reference (tests/fixtures/twitch-mock.html):
- Dark theme body background #0e0e10
- Layout with stream area + right column for chat slot
- Minimal but structurally representative
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kick-mock.html fixture and test-kick-detection + test-kick-injection specs</name>
  <files>tests/fixtures/kick-mock.html, tests/test-kick-detection.spec.ts, tests/test-kick-injection.spec.ts</files>
  <action>
Create tests/fixtures/kick-mock.html: A minimal Kick stream page mock that includes all three chat slot selectors and a live badge. Structure:
- Dark background #0c0c0e
- Flex layout: stream area (left) + right column containing `#channel-chatroom`
- Inside `#channel-chatroom`: also nest `#chatroom` > `.chatroom-wrapper` so all fallback selectors exist
- Add `<span data-state="live" class="live-badge">LIVE</span>` in the header area
- Add `<title>kaicenat - Kick</title>` (title mutation tests need this)

Create tests/test-kick-detection.spec.ts with skip-stubs for KICK-01:
- `test.skip('KICK-01a: isLiveStream() returns true when [data-state="live"] badge present', ...)`
- `test.skip('KICK-01b: isLiveStream() returns true when #channel-chatroom exists and is visible (fallback)', ...)`
- `test.skip('KICK-01c: isLiveStream() returns false and emits console.warn when no live signal found', ...)`
- `test('KICK-01d: kick.ts content script does not inject on non-live Kick page', async ({ page }) => { test.skip(); })` (needs fixture)

Create tests/test-kick-injection.spec.ts with skip-stubs for KICK-02:
- `test('KICK-02a: #allchat-container appears inside #channel-chatroom on live Kick page', async ({ page }) => { test.skip(); })`
- `test('KICK-02b: native Kick chat hidden via injected style tag with id allchat-hide-native-style', async ({ page }) => { test.skip(); })`
- `test('KICK-02c: iframe inside #allchat-container has data-platform="kick"', async ({ page }) => { test.skip(); })`
  </action>
  <verify>
    <automated>npx playwright test tests/test-kick-detection.spec.ts tests/test-kick-injection.spec.ts --project=chromium-extension 2>&1 | tail -5</automated>
  </verify>
  <done>Both spec files created with skip-stubs; playwright reports all tests as skipped (0 failures). kick-mock.html exists with #channel-chatroom, #chatroom, .chatroom-wrapper, and [data-state="live"] elements.</done>
</task>

<task type="auto">
  <name>Task 2: Create test-postmessage-origin, test-kick-spa-navigation, and test-kick-selector-fallback specs</name>
  <files>tests/test-postmessage-origin.spec.ts, tests/test-kick-spa-navigation.spec.ts, tests/test-kick-selector-fallback.spec.ts</files>
  <action>
Create tests/test-postmessage-origin.spec.ts with skip-stubs for KICK-05:
- `test.skip('KICK-05a: PlatformDetector.ts injectAllChatUI uses extensionOrigin not "*" as targetOrigin', ...)`
  — fs test: read src/content-scripts/base/PlatformDetector.ts, assert it does NOT contain `postMessage(` followed by `'*'`
- `test.skip('KICK-05b: twitch.ts setupGlobalMessageRelay relay uses extensionOrigin not "*"', ...)`
  — fs test: read src/content-scripts/twitch.ts, assert relay postMessage does NOT use `'*'`
- `test.skip('KICK-05c: youtube.ts setupGlobalMessageRelay relay uses extensionOrigin not "*"', ...)`
  — fs test: same for youtube.ts
- `test.skip('KICK-05d: src/ui/index.tsx message listener has origin guard before processing', ...)`
  — fs test: read src/ui/index.tsx, assert contains `event.origin !== extensionOrigin`
- `test('KICK-05e: iframe rejects postMessage from non-extension origin', async ({ page }) => { test.skip(); })`

Create tests/test-kick-spa-navigation.spec.ts with skip-stubs for KICK-06:
- `test('KICK-06a: navigating from one Kick channel to another tears down existing #allchat-container', async ({ page }) => { test.skip(); })`
- `test('KICK-06b: SPA navigation re-injects iframe in new channel chat slot', async ({ page }) => { test.skip(); })`
- `test('KICK-06c: double-navigation does not create duplicate #allchat-container elements', async ({ page }) => { test.skip(); })`

Create tests/test-kick-selector-fallback.spec.ts with skip-stubs for KICK-07:
- `test.skip('KICK-07a: kick.ts source contains date-comment on each selector in fallback chain', ...)`
  — fs test: read src/content-scripts/kick.ts (once created), assert contains `#channel-chatroom` + date string
- `test('KICK-07b: injection succeeds when only #chatroom exists (primary selector absent)', async ({ page }) => { test.skip(); })`
- `test('KICK-07c: injection succeeds when only .chatroom-wrapper exists (both ID selectors absent)', async ({ page }) => { test.skip(); })`
  </action>
  <verify>
    <automated>npx playwright test tests/test-postmessage-origin.spec.ts tests/test-kick-spa-navigation.spec.ts tests/test-kick-selector-fallback.spec.ts --project=chromium-extension 2>&1 | tail -5</automated>
  </verify>
  <done>Three spec files created with skip-stubs; playwright reports all tests as skipped (0 failures). All six Wave 0 files now exist.</done>
</task>

</tasks>

<verification>
Full suite stays green after Wave 0:
```
npx playwright test --project=chromium-extension
```
All new tests report as skipped, no errors. Total test count increases by the new stubs.
</verification>

<success_criteria>
- 6 files created: tests/fixtures/kick-mock.html, tests/test-kick-detection.spec.ts, tests/test-kick-injection.spec.ts, tests/test-postmessage-origin.spec.ts, tests/test-kick-spa-navigation.spec.ts, tests/test-kick-selector-fallback.spec.ts
- All five spec files have skip-stubs matching the KICK requirement IDs they cover
- kick-mock.html contains #channel-chatroom, #chatroom, .chatroom-wrapper, and [data-state="live"] elements
- npx playwright test --project=chromium-extension exits 0 (no failures, only skips for new tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-kick-platform/03-01-SUMMARY.md`
</output>
