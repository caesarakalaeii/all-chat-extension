---
phase: 07-twitch-native-widget-extraction
plan: 05
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
  - "07-04"
files_modified:
  - README.md
autonomous: false
requirements:
  - WIDGET-01
  - WIDGET-02
  - WIDGET-03
  - WIDGET-04
  - WIDGET-05
  - WIDGET-06
  - WIDGET-07
  - WIDGET-08
must_haves:
  truths:
    - "User has verified tab bar and widget extraction on live Twitch"
    - "YouTube and Kick have no regressions"
    - "Pop-out mode renders full ChatContainer header (not tabBarMode); pop-out button in floating position when on Twitch in-page; closing pop-out restores in-page chat without regression"
  artifacts:
    - path: "README.md"
      provides: "Updated documentation reflecting tab bar and widget zone features for Twitch"
      contains: "tab bar"
  key_links: []
---

<objective>
Human verification of the complete Phase 7 implementation on live Twitch, YouTube, Kick, and pop-out mode. Also updates README.md to document the new tab bar and widget extraction features.

Purpose: Widget extraction, tab bar switching, and event forwarding cannot be fully verified in automated tests -- live Twitch DOM is required to confirm selector accuracy, clone visual fidelity, and event forwarding interactivity. README update keeps project documentation current per global CLAUDE.md requirements.

Output: User approval or issue list for gap closure; updated README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-twitch-native-widget-extraction/07-CONTEXT.md
@.planning/phases/07-twitch-native-widget-extraction/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build production extension and run full test suite</name>
  <files>dist/</files>
  <read_first>
    - package.json (build script)
  </read_first>
  <action>
Run `npm run build` to produce the production extension bundle. This is the build the user will load into Chrome for manual testing.

Also run `npm test` to confirm the full test suite passes before human verification.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat-extension && npm run build 2>&1 | tail -3 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - npm test exits 0 (all tests pass, new stubs show as skipped)
  </acceptance_criteria>
  <done>Production build ready for manual testing; all automated tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with tab bar and widget extraction documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state)
    - .planning/phases/07-twitch-native-widget-extraction/07-CONTEXT.md (feature summary for documentation)
  </read_first>
  <action>
Update README.md to document the Phase 7 Twitch tab bar and widget extraction features. Add a section (or update the existing Twitch section) describing:

1. **Tab bar switcher** -- On Twitch, AllChat now shows a persistent tab bar (`[AllChat] | [Twitch Chat]`) at the top of the chat area instead of the old small "Switch to native" button. Users can instantly toggle between AllChat cross-platform chat and native Twitch chat.

2. **Widget extraction** -- When using AllChat on Twitch, native interactive widgets (channel points, predictions, polls, hype trains, raids) are automatically extracted from the native chat and displayed alongside the AllChat iframe. Users retain full interactivity -- clicking widgets in the AllChat view triggers the real Twitch actions.

3. **Connection indicator** -- The tab bar shows a connection status dot next to the AllChat label indicating live connection state.

Keep the documentation concise and user-facing. Do not include implementation details (cloneNode, MutationObserver, etc.) -- focus on what the user sees and can do.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat-extension && grep -c 'tab bar\|widget' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md mentions the tab bar feature for Twitch
    - README.md mentions widget extraction (channel points, predictions)
    - README.md does not include internal implementation details
  </acceptance_criteria>
  <done>README.md updated with user-facing documentation of Twitch tab bar and widget extraction features</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify tab bar, widget extraction, and regression on all platforms</name>
  <action>Present the verification checklist to the user and wait for approval. The user will manually test the extension on live Twitch, YouTube, Kick, and pop-out mode.</action>
  <what-built>
Tab bar switcher ([Infinity AllChat] | [Twitch Chat]) replacing the iframe header on Twitch, with widget zones that extract native Twitch interactive widgets (channel points, predictions, polls, hype trains) from the hidden native chat and display them alongside AllChat.
  </what-built>
  <how-to-verify>
**Load the extension:**
1. Open `chrome://extensions`, enable Developer mode
2. Click "Load unpacked" and select the `dist/` directory
3. (Or reload if already loaded)

**Test 1: Tab bar on Twitch (D-04, D-05, D-06)**
1. Navigate to a live Twitch stream (e.g., twitch.tv/etro or any AllChat-enabled streamer)
2. Verify: Tab bar appears at top of chat area with two tabs: [Infinity AllChat] | [Twitch Chat]
3. Verify: Tab bar uses dark theme styling (not Twitch purple)
4. Verify: Connection dot visible in AllChat tab label
5. Verify: InfinityLogo (infinity symbol) visible before "AllChat" text

**Test 2: Tab switching (D-07)**
1. Click "Twitch Chat" tab
2. Verify: AllChat iframe disappears, native Twitch chat is fully visible
3. Verify: Tab bar stays at top (does NOT disappear)
4. Verify: "Twitch Chat" tab shows active indicator (purple bottom border)
5. Click "AllChat" tab
6. Verify: AllChat iframe reappears, native Twitch chat is hidden again
7. Verify: Chat messages are still present (no data loss on switch)
8. Verify: No old "Switch to native" button visible anywhere

**Test 3: Channel points (D-15)**
1. While on AllChat tab, check bottom of chat area
2. Verify: Channel points balance is visible (if logged into Twitch)
3. If a "claim bonus" button appears, try clicking it
4. Verify: Points are actually claimed (balance updates)

**Test 4: Predictions/Polls (D-14, D-16)**
(Only testable if the streamer runs a prediction or poll -- skip if not available)
1. When a prediction appears, verify it shows in the top widget zone
2. Try voting on the prediction clone
3. Verify: Vote registers on the real prediction

**Test 5: Pop-out mode**
1. Click the pop-out button (small arrow icon, top-right of iframe when on AllChat tab)
2. Verify: Pop-out window opens with full header (NOT tabBarMode -- shows InfinityLogo center, connection dot, platform badge)
3. Verify: Pop-out button is in floating position (top-right) when viewing Twitch in-page (tabBarMode)
4. Close pop-out, verify in-page chat returns normally without regression

**Test 6: YouTube regression check**
1. Navigate to a live YouTube stream with AllChat enabled
2. Verify: Normal iframe header appears (InfinityLogo center, connection dot, etc.)
3. Verify: "Switch to native" button works as before
4. Verify: No tab bar appears on YouTube (tab bar is Twitch-only)

**Test 7: Kick regression check**
1. Navigate to a live Kick stream with AllChat enabled
2. Verify: Normal iframe header, switch to native works, no tab bar
  </how-to-verify>
  <verify>
    <automated>echo "Human verification -- no automated command"</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" confirming all 7 tests pass
  </acceptance_criteria>
  <done>User has verified tab bar, widget extraction, and no regressions on all platforms</done>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries -- verification and documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | Verification and documentation plan only -- no production code changes |
</threat_model>

<verification>
Human verification of all Phase 7 features on live platforms. README.md updated.
</verification>

<success_criteria>
- User approves tab bar appearance and switching behavior
- Widget extraction works for at least channel points
- No regressions on YouTube, Kick, or pop-out mode
- All automated tests pass
- README.md documents the new Twitch features
</success_criteria>

<output>
After completion, create `.planning/phases/07-twitch-native-widget-extraction/07-05-SUMMARY.md`
</output>
