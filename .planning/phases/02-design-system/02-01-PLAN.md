---
phase: 02-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-design-system.spec.ts
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04, DS-05, DS-06, DS-07, DS-08, DS-09, DS-10]

must_haves:
  truths:
    - "test-design-system.spec.ts exists with a stub test for each DS-0x requirement"
    - "All stub tests are skipped (not failing) so the suite stays green before implementation"
    - "The file imports Playwright and uses frameLocator patterns for iframe assertions"
  artifacts:
    - path: "tests/test-design-system.spec.ts"
      provides: "Wave 0 test scaffold covering DS-01 through DS-10"
      exports: []
  key_links:
    - from: "tests/test-design-system.spec.ts"
      to: "dist/ui/chat-styles.css"
      via: "build-artifact assertions (fs.existsSync + grep-equivalent)"
      pattern: "existsSync.*chat-styles"
---

<objective>
Create the Wave 0 test scaffold for Phase 2. This plan produces
`tests/test-design-system.spec.ts` — a Playwright spec file with a
dedicated stub test for each of the ten DS-0x requirements. Every stub
is marked `test.skip` so the suite stays green while implementation
plans execute.

Purpose: Nyquist compliance — every implementation task must have a
corresponding automated verify command. This scaffold provides the
verification targets that plans 02-05 will make green.

Output: `tests/test-design-system.spec.ts` (committed, suite green)
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system/02-VALIDATION.md

<interfaces>
<!-- Playwright test framework already in use. Existing specs use: -->
<!-- npx playwright test --project=chromium-extension -->
<!-- Extension loaded via persistent context in playwright.config.ts -->

<!-- DS-01 test: build smoke + file absence -->
<!-- DS-02 test: grep oklch count in dist/ui/chat-styles.css -->
<!-- DS-03 test: ls dist/ui/chat-styles.css + grep 'chat-styles.css' dist/ui/chat-container.html -->
<!-- DS-04 test: page.evaluate(() => getComputedStyle(document.documentElement).getPropertyValue('--color-neutral-900')) returns '' -->
<!-- DS-05 test: frameLocator('iframe[data-platform]').locator('svg').first() is visible -->
<!-- DS-06 test: frameLocator body computed font-family contains 'Inter' -->
<!-- DS-07 test: frameLocator evaluate getPropertyValue('--color-twitch') returns '#A37BFF' -->
<!-- DS-08 test: grep -c autoprefixer postcss.config.js returns 0 -->
<!-- DS-09 test: node -e check tailwind-merge version >= 3 -->
<!-- DS-10 test: force error in iframe child, assert fallback text visible via frameLocator -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-design-system.spec.ts with DS-01 through DS-10 stubs</name>
  <files>tests/test-design-system.spec.ts</files>
  <action>
Create `tests/test-design-system.spec.ts` as a Playwright spec file. Structure:

1. Import `test`, `expect` from `@playwright/test` and `fs`, `path` from Node builtins.

2. Create a `test.describe('Design System — Phase 2')` block.

3. For each requirement DS-01 through DS-10, write one `test.skip(...)` test:

   **DS-01** — "DS-01: tailwind.config.js deleted and Tailwind 4 in postcss.config.js"
   - `test.skip('DS-01: ...')` — body: assert `!fs.existsSync(path.resolve(__dirname, '../tailwind.config.js'))` and `fs.readFileSync('../postcss.config.js','utf8').includes('@tailwindcss/postcss')`

   **DS-02** — "DS-02: dist/ui/chat-styles.css contains oklch values"
   - body: read `dist/ui/chat-styles.css`, assert content includes `oklch(`

   **DS-03** — "DS-03: chat-styles.css exists and chat-container.html links it"
   - body: assert file exists + `dist/ui/chat-container.html` content includes `chat-styles.css`

   **DS-04** — "DS-04: platform page :root has no --color-neutral-* tokens after iframe injection"
   - body: use Playwright `page` fixture + `page.goto` fixture page, then `page.evaluate(() => getComputedStyle(document.documentElement).getPropertyValue('--color-neutral-900'))` and assert it equals `''`
   - Note: DS-04 needs page fixture — mark `test.skip` with a dummy async fn `async ({ page }) => { test.skip() }`

   **DS-05** — "DS-05: InfinityLogo SVG is visible in chat header inside iframe"
   - body: `async ({ page }) => { test.skip() }` with comment "Requires built extension + fixture page"

   **DS-06** — "DS-06: Inter font applied to iframe body"
   - body: `async ({ page }) => { test.skip() }` with comment "Requires built extension + fixture page"

   **DS-07** — "DS-07: platform accent colors match WCAG-AA spec"
   - body: `async ({ page }) => { test.skip() }` with comment "Requires built extension"

   **DS-08** — "DS-08: autoprefixer removed from postcss.config.js"
   - body: sync fs check — `fs.readFileSync` postcss.config.js, assert does NOT include `autoprefixer`

   **DS-09** — "DS-09: tailwind-merge at v3+"
   - body: sync check — `JSON.parse(fs.readFileSync('../node_modules/tailwind-merge/package.json','utf8')).version` major >= 3

   **DS-10** — "DS-10: ErrorBoundary shows fallback card on render error"
   - body: `async ({ page }) => { test.skip() }` with comment "Requires built extension + error injection"

4. All ten tests must be `test.skip(...)` — NOT `test(...)` with empty bodies. Use the static skip form (no callback) for sync checks that are not yet runnable; use `async ({ page }) => { test.skip() }` only for tests that need the Playwright fixture.

   IMPORTANT: For sync filesystem tests (DS-01, DS-02, DS-03, DS-08, DS-09) use the static form:
   ```typescript
   test.skip('DS-01: ...', () => {
     // assertions here
   });
   ```
   This causes Playwright to skip without running the body.

5. At the bottom of the describe block, add a comment:
   ```
   // Wave 0 scaffold — remove test.skip as each DS requirement is implemented
   ```

After creating the file, run `npx playwright test tests/test-design-system.spec.ts --project=chromium-extension 2>&1 | tail -5` to confirm all 10 tests are skipped (not failed).
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat-extension && npx playwright test tests/test-design-system.spec.ts --project=chromium-extension 2>&1 | tail -10</automated>
  </verify>
  <done>10 tests reported as skipped, 0 failed, 0 errors. File exists at tests/test-design-system.spec.ts.</done>
</task>

</tasks>

<verification>
`npx playwright test tests/test-design-system.spec.ts --project=chromium-extension` reports 10 skipped, 0 failed.
</verification>

<success_criteria>
- tests/test-design-system.spec.ts exists with one skip-stub per DS-01 through DS-10
- Full Playwright suite still green (no regressions from new file)
- File uses frameLocator pattern for iframe tests (DS-04 through DS-07, DS-10)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-01-SUMMARY.md`
</output>
