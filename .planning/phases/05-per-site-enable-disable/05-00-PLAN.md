---
phase: 05-per-site-enable-disable
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test-per-site-enable.spec.ts
  - tests/test-storage-migration.spec.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "Skip-stub specs exist for per-site enable/disable behavioral tests"
    - "Skip-stub specs exist for storage migration tests"
    - "Running npx playwright test --grep @phase5 succeeds (all tests skipped)"
  artifacts:
    - path: "tests/test-per-site-enable.spec.ts"
      provides: "Skip-stub E2E specs for per-platform toggle behaviors"
      contains: "test.skip"
    - path: "tests/test-storage-migration.spec.ts"
      provides: "Skip-stub specs for storage migration"
      contains: "test.skip"
  key_links: []
---

<objective>
Create skip-stub test files for all Phase 5 behavioral tests so that Plans 01-03 can run `npx playwright test --grep @phase5` as part of their verify blocks.

Purpose: VALIDATION.md identifies all five test entries as Wave 0 gaps. Without these stubs, Plans 01-03 have no behavioral test coverage and rely solely on `npx tsc --noEmit`. This plan provides the test scaffolding that later plans will unskip and flesh out.

Output: Two test files with skip-stubs covering all D-01 through D-08 behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-per-site-enable-disable/05-CONTEXT.md
@.planning/phases/05-per-site-enable-disable/05-RESEARCH.md
@.planning/phases/05-per-site-enable-disable/05-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure patterns -->
From tests/test-slot-injection.spec.ts:
```typescript
import { test, expect, chromium, BrowserContext } from '@playwright/test';
import path from 'path';

const EXTENSION_PATH = path.resolve(__dirname, '../dist');

async function launchExtensionContext(): Promise<BrowserContext> {
  return chromium.launchPersistentContext('', {
    headless: false,
    args: [
      `--disable-extensions-except=${EXTENSION_PATH}`,
      `--load-extension=${EXTENSION_PATH}`,
      '--no-sandbox',
    ],
    viewport: { width: 1280, height: 800 },
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skip-stub test files for per-site enable/disable and storage migration</name>
  <files>tests/test-per-site-enable.spec.ts, tests/test-storage-migration.spec.ts</files>
  <read_first>
    - tests/test-slot-injection.spec.ts
    - tests/test-spa-navigation.spec.ts
    - .planning/phases/05-per-site-enable-disable/05-VALIDATION.md
  </read_first>
  <action>
    Create two test files with skip-stubs following existing test file conventions.

    **File 1: tests/test-per-site-enable.spec.ts**

    ```typescript
    import { test, expect, chromium, BrowserContext } from '@playwright/test';
    import path from 'path';

    const EXTENSION_PATH = path.resolve(__dirname, '../dist');

    async function launchExtensionContext(): Promise<BrowserContext> {
      return chromium.launchPersistentContext('', {
        headless: false,
        args: [
          `--disable-extensions-except=${EXTENSION_PATH}`,
          `--load-extension=${EXTENSION_PATH}`,
          '--no-sandbox',
        ],
        viewport: { width: 1280, height: 800 },
      });
    }

    test.describe('Per-site enable/disable @phase5', () => {
      test.skip('popup shows three platform toggle rows', async () => {
        // D-03: Three per-platform toggles in popup
      });

      test.skip('disabling a platform prevents injection', async () => {
        // D-04: Toggle takes effect immediately
      });

      test.skip('re-enabling a platform restores injection without reload', async () => {
        // D-04: No page reload on re-enable
      });

      test.skip('disabling one platform does not affect another', async () => {
        // D-01: Per-platform granularity
      });

      test.skip('current platform row is highlighted in popup', async () => {
        // D-05: Active row highlight
      });

      test.skip('default: all platforms enabled on fresh storage', async () => {
        // D-06: All three platforms enabled by default
      });

      test.skip('toolbar icon is grayscale when platform is disabled', async () => {
        // D-07, D-08: Grayscale icon feedback
      });
    });
    ```

    **File 2: tests/test-storage-migration.spec.ts**

    ```typescript
    import { test, expect, chromium, BrowserContext } from '@playwright/test';
    import path from 'path';

    const EXTENSION_PATH = path.resolve(__dirname, '../dist');

    async function launchExtensionContext(): Promise<BrowserContext> {
      return chromium.launchPersistentContext('', {
        headless: false,
        args: [
          `--disable-extensions-except=${EXTENSION_PATH}`,
          `--load-extension=${EXTENSION_PATH}`,
          '--no-sandbox',
        ],
        viewport: { width: 1280, height: 800 },
      });
    }

    test.describe('Storage migration @phase5', () => {
      test.skip('legacy extensionEnabled=true migrates to all platforms enabled', async () => {
        // D-02: extensionEnabled -> platformEnabled migration
      });

      test.skip('legacy extensionEnabled=false migrates to all platforms disabled', async () => {
        // D-02: extensionEnabled -> platformEnabled migration
      });

      test.skip('partial platformEnabled is deep-merged with defaults', async () => {
        // D-02: Deep merge for partial objects
      });

      test.skip('migration removes legacy extensionEnabled key from storage', async () => {
        // D-02: Clean up legacy key
      });
    });
    ```

    Both files use `test.skip()` so they register as skipped (not failures) when run. The `@phase5` tag in the describe block name allows filtering with `--grep @phase5`.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat-extension && npx playwright test --grep @phase5 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-per-site-enable.spec.ts exists with 7 skip-stubs
    - tests/test-storage-migration.spec.ts exists with 4 skip-stubs
    - Both files contain `@phase5` in describe block name
    - `npx playwright test --grep @phase5` exits 0 (all skipped)
  </acceptance_criteria>
  <done>Skip-stub test files exist for all Phase 5 behavioral tests; running --grep @phase5 succeeds with all tests in skipped state</done>
</task>

</tasks>

<verification>
- `npx playwright test --grep @phase5` exits with code 0
- Both test files exist in tests/ directory
- All stubs use `test.skip()` pattern
</verification>

<success_criteria>
- Wave 0 test scaffolding complete for all Phase 5 behaviors
- Plans 01-03 can now include `npx playwright test --grep @phase5` in their verify blocks
</success_criteria>

<output>
After completion, create `.planning/phases/05-per-site-enable-disable/05-00-SUMMARY.md`
</output>
