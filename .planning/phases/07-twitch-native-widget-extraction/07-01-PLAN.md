---
phase: 07-twitch-native-widget-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-tab-bar.spec.ts
  - tests/test-widget-zones.spec.ts
  - tests/fixtures/twitch-mock.html
autonomous: true
requirements:
  - WIDGET-01
  - WIDGET-02
  - WIDGET-03
  - WIDGET-04
  - WIDGET-05
  - WIDGET-06
  - WIDGET-07
  - WIDGET-08
must_haves:
  truths:
    - "test-tab-bar.spec.ts contains skip-stubs for WIDGET-01 through WIDGET-04"
    - "test-widget-zones.spec.ts contains skip-stubs for WIDGET-05 through WIDGET-07"
    - "twitch-mock.html contains mock widget DOM elements for channel points and predictions"
    - "npm test passes with all new stubs skipped"
  artifacts:
    - path: "tests/test-tab-bar.spec.ts"
      provides: "Tab bar E2E test stubs"
      contains: "WIDGET-01"
    - path: "tests/test-widget-zones.spec.ts"
      provides: "Widget zone E2E test stubs"
      contains: "WIDGET-05"
    - path: "tests/fixtures/twitch-mock.html"
      provides: "Mock Twitch fixture with widget DOM"
      contains: "community-points-summary"
  key_links: []
---

<objective>
Create Wave 0 test scaffolding for all Phase 7 requirements and update the Twitch mock fixture with widget DOM elements.

Purpose: Establishes the test contract before any implementation — test stubs define acceptance criteria for tab bar switching and widget zone injection. The updated fixture provides mock Twitch widget DOM elements (channel points, prediction card) so E2E tests can exercise widget detection without live Twitch.

Output: Three files — two test specs with skip-stubs, one updated fixture HTML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-twitch-native-widget-extraction/07-CONTEXT.md
@.planning/phases/07-twitch-native-widget-extraction/07-RESEARCH.md
@.planning/phases/07-twitch-native-widget-extraction/07-UI-SPEC.md

@tests/fixtures/twitch-mock.html
@tests/test-switch-native.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-tab-bar.spec.ts and test-widget-zones.spec.ts with skip-stubs</name>
  <files>tests/test-tab-bar.spec.ts, tests/test-widget-zones.spec.ts</files>
  <read_first>
    - tests/test-switch-native.spec.ts (pattern reference for skip-stub format)
    - tests/test-slot-injection.spec.ts (pattern reference for Playwright extension test structure)
    - .planning/phases/07-twitch-native-widget-extraction/07-RESEARCH.md (Validation Architecture section — requirement-to-test mapping)
  </read_first>
  <action>
Create two new Playwright test files following the established project convention (import { test } from '@playwright/test', test.describe blocks, test.skip() stubs).

**tests/test-tab-bar.spec.ts:**
```typescript
import { test } from '@playwright/test';

test.describe('Tab bar switcher', () => {
  test.skip('WIDGET-01: Tab bar appears in .chat-shell on Twitch pages', async () => {
    // Verify #allchat-tab-bar exists as direct child of .chat-shell
    // Verify it contains two tab buttons: #allchat-tab-allchat and #allchat-tab-twitch
    // Verify role="tablist" and aria-label="Chat view switcher" on container
  });

  test.skip('WIDGET-02: Clicking Twitch Chat tab hides AllChat iframe and shows native chat', async () => {
    // Click #allchat-tab-twitch
    // Verify #allchat-container has display: none
    // Verify #allchat-hide-native-style element is removed from DOM
    // Verify #allchat-tab-twitch has aria-selected="true"
  });

  test.skip('WIDGET-03: Clicking AllChat tab restores AllChat iframe and hides native chat', async () => {
    // Switch to Twitch tab first, then click #allchat-tab-allchat
    // Verify #allchat-container is visible (not display: none)
    // Verify #allchat-hide-native-style is present in head
    // Verify #allchat-tab-allchat has aria-selected="true"
  });

  test.skip('WIDGET-04: Tab bar persists when native chat tab is active', async () => {
    // Click #allchat-tab-twitch to switch to native
    // Verify #allchat-tab-bar is still visible (not display: none)
    // Verify tab bar z-index places it above native chat
  });
});
```

**tests/test-widget-zones.spec.ts:**
```typescript
import { test } from '@playwright/test';

test.describe('Widget zone injection', () => {
  test.skip('WIDGET-05: Widget zones injected into #allchat-container DOM', async () => {
    // Verify #allchat-widget-zone-top exists as child of #allchat-container
    // Verify #allchat-widget-zone-bottom exists as child of #allchat-container
    // Verify #allchat-iframe-wrapper exists between the two zones
    // Verify flex layout on #allchat-container
  });

  test.skip('WIDGET-06: Channel points widget clone appears in bottom zone', async () => {
    // Inject mock [data-test-selector="community-points-summary"] element into fixture
    // Verify a clone appears inside #allchat-widget-zone-bottom
    // Verify clone has aria-hidden="true"
  });

  test.skip('WIDGET-07: Transient widget clone appears and disappears with original', async () => {
    // Inject mock prediction card element into fixture DOM
    // Verify clone appears in #allchat-widget-zone-top
    // Remove original prediction element from fixture DOM
    // Verify clone is removed from #allchat-widget-zone-top
  });
});

test.describe('Regression', () => {
  test.skip('WIDGET-08: Existing test suite still passes (no regressions)', async () => {
    // This is verified by running npm test — all prior specs must still pass
    // No implementation needed; acts as a reminder during verification
  });
});
```
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat-extension && npx playwright test tests/test-tab-bar.spec.ts tests/test-widget-zones.spec.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-tab-bar.spec.ts contains `test.skip('WIDGET-01:`
    - tests/test-tab-bar.spec.ts contains `test.skip('WIDGET-02:`
    - tests/test-tab-bar.spec.ts contains `test.skip('WIDGET-03:`
    - tests/test-tab-bar.spec.ts contains `test.skip('WIDGET-04:`
    - tests/test-widget-zones.spec.ts contains `test.skip('WIDGET-05:`
    - tests/test-widget-zones.spec.ts contains `test.skip('WIDGET-06:`
    - tests/test-widget-zones.spec.ts contains `test.skip('WIDGET-07:`
    - tests/test-widget-zones.spec.ts contains `test.skip('WIDGET-08:`
    - All 8 tests appear as skipped in Playwright output (0 failed)
  </acceptance_criteria>
  <done>Eight skip-stub tests exist across two files covering WIDGET-01 through WIDGET-08; all skipped, zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Update twitch-mock.html with mock Twitch widget DOM elements</name>
  <files>tests/fixtures/twitch-mock.html</files>
  <read_first>
    - tests/fixtures/twitch-mock.html (current state — must preserve existing .chat-shell structure)
    - tests/fixtures/kick-mock.html (reference for how fixtures include mock widget/badge elements)
    - .planning/phases/07-twitch-native-widget-extraction/07-RESEARCH.md (Twitch Widget Selector Reference section — selector names to use)
  </read_first>
  <action>
Add mock Twitch widget DOM elements inside the `.chat-shell` div in twitch-mock.html. These elements simulate the native Twitch widgets that the widget extraction system will detect and clone.

Add these elements INSIDE `.chat-shell`, as siblings of the existing `.stream-chat` div:

1. **Channel points widget (persistent)** — placed at the bottom of `.chat-shell`:
```html
<!-- Mock Twitch channel points widget (persistent) — 2026-04 selectors -->
<div class="community-points-summary" data-test-selector="community-points-summary">
  <div class="community-points-summary__content">
    <span data-test-selector="copo-balance-string">12,450</span>
    <button data-test-selector="community-points-summary__claim-button" class="tw-button">Claim Bonus</button>
  </div>
</div>
```

2. **Prediction widget (transient)** — placed at the top of `.chat-shell`, before `.stream-chat`:
```html
<!-- Mock Twitch prediction widget (transient) — 2026-04 selectors -->
<div class="prediction-checkout-widget" data-test-selector="community-prediction-highlight-header" id="mock-prediction-widget" style="display: none;">
  <div data-test-selector="community-prediction-highlight-header-title">Who will win?</div>
  <div class="prediction-options">
    <button data-test-selector="predictions-list-item__title">Option A</button>
    <button data-test-selector="predictions-list-item__title">Option B</button>
  </div>
</div>
```

The prediction widget starts with `display: none` so tests can show/hide it to simulate transient widget appearance/disappearance.

Also add a small CSS block for the mock widgets so they have minimal visible styling:
```css
.community-points-summary {
  padding: 8px;
  background: #1f1f23;
  border-top: 1px solid #2c2c34;
  font-size: 13px;
}
.prediction-checkout-widget {
  padding: 8px;
  background: #1f1f23;
  border-bottom: 1px solid #2c2c34;
  font-size: 13px;
}
```

Preserve all existing HTML structure. Only add to it.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat-extension && grep -c 'community-points-summary' tests/fixtures/twitch-mock.html && grep -c 'community-prediction-highlight-header' tests/fixtures/twitch-mock.html && grep -c 'chat-shell' tests/fixtures/twitch-mock.html</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/twitch-mock.html contains `data-test-selector="community-points-summary"`
    - tests/fixtures/twitch-mock.html contains `data-test-selector="copo-balance-string"`
    - tests/fixtures/twitch-mock.html contains `data-test-selector="community-points-summary__claim-button"`
    - tests/fixtures/twitch-mock.html contains `data-test-selector="community-prediction-highlight-header"`
    - tests/fixtures/twitch-mock.html contains `data-test-selector="predictions-list-item__title"`
    - tests/fixtures/twitch-mock.html contains `id="mock-prediction-widget"`
    - tests/fixtures/twitch-mock.html still contains `class="chat-shell"` (existing structure preserved)
    - tests/fixtures/twitch-mock.html still contains `class="stream-chat"` (existing structure preserved)
    - npm test passes (no regressions from fixture change)
  </acceptance_criteria>
  <done>twitch-mock.html contains mock channel points and prediction widget DOM with data-test-selector attributes matching Twitch conventions; existing fixture structure preserved; npm test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Test scaffolding and fixture updates only — no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I (Information Disclosure) | twitch-mock.html | accept | Fixture HTML is test-only, not shipped in extension bundle; no sensitive data |
</threat_model>

<verification>
Run `npm test` to verify all existing tests pass and new test stubs show as skipped (not failed).
Run `npx playwright test tests/test-tab-bar.spec.ts tests/test-widget-zones.spec.ts` to verify new stubs are recognized.
</verification>

<success_criteria>
- 8 new skip-stub tests across 2 files (WIDGET-01 through WIDGET-08)
- twitch-mock.html has channel points + prediction mock widget DOM
- npm test passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/07-twitch-native-widget-extraction/07-01-SUMMARY.md`
</output>
