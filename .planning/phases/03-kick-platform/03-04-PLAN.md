---
phase: 03-kick-platform
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - manifest.json
  - webpack.config.js
autonomous: true
requirements:
  - KICK-03
  - KICK-04

must_haves:
  truths:
    - "manifest.json declares Kick content_scripts entry (matches: https://kick.com/*)"
    - "manifest.json includes https://kick.com/* in host_permissions"
    - "manifest.json includes kick.com/* in web_accessible_resources matches"
    - "webpack.config.js has content-scripts/kick entry pointing to src/content-scripts/kick.ts"
    - "npm run build succeeds and dist/content-scripts/kick.js is produced"
  artifacts:
    - path: "manifest.json"
      provides: "Kick content_scripts + host_permissions + web_accessible_resources entries"
      contains: "kick.com"
    - path: "webpack.config.js"
      provides: "content-scripts/kick webpack entry"
      contains: "content-scripts/kick"
    - path: "dist/content-scripts/kick.js"
      provides: "Built Kick content script bundle"
  key_links:
    - from: "manifest.json content_scripts[].matches"
      to: "https://kick.com/*"
      via: "manifest host_permissions must match content_scripts matches exactly"
      pattern: "kick\\.com"
    - from: "webpack.config.js entry"
      to: "src/content-scripts/kick.ts"
      via: "content-scripts/kick entry key"
      pattern: "content-scripts/kick"
---

<objective>
Wire the Kick content script into manifest.json and webpack.config.js so the extension loads on kick.com and the built dist includes kick.js.

Purpose: KICK-03 and KICK-04 — Without these two file changes, the extension never loads on kick.com regardless of how correct kick.ts is.
Output: 2 modified files. After `npm run build`, dist/content-scripts/kick.js exists.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-kick-platform/03-RESEARCH.md
@.planning/phases/03-kick-platform/03-03-SUMMARY.md

<interfaces>
<!-- Current manifest.json structure (exact current state — do NOT change existing entries): -->

host_permissions (add kick.com line):
```json
"host_permissions": [
  "https://www.twitch.tv/*",
  "https://www.youtube.com/*",
  "https://kick.com/*",          // ADD — no www subdomain
  "http://localhost:8080/*",
  "https://allch.at/*"
]
```

content_scripts (add kick entry after youtube entry):
```json
{
  "matches": ["https://kick.com/*"],
  "js": ["content-scripts/kick.js"],
  "css": ["content-scripts/styles.css"],
  "run_at": "document_idle"
}
```

web_accessible_resources matches (add kick.com/* to existing array):
```json
"matches": [
  "https://www.twitch.tv/*",
  "https://www.youtube.com/*",
  "https://kick.com/*"           // ADD
]
```

NOTE: web_accessible_resources uses "kick.com/*" format (no scheme — WAR format differs
from host_permissions). Both formats must be consistent: host_permissions and
content_scripts.matches use "https://kick.com/*"; WAR matches use "https://kick.com/*"
(actually WAR also accepts full URLs — use "https://kick.com/*" for consistency).

<!-- Current webpack.config.js entry object (add kick line): -->
```javascript
entry: {
  'background': './src/background/service-worker.ts',
  'content-scripts/twitch': './src/content-scripts/twitch.ts',
  'content-scripts/youtube': './src/content-scripts/youtube.ts',
  'content-scripts/kick': './src/content-scripts/kick.ts',   // ADD
  'ui/chat-bundle': './src/ui/index.tsx',
  'popup/popup': './src/popup/popup.tsx'
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update manifest.json and webpack.config.js with Kick entries</name>
  <files>manifest.json, webpack.config.js</files>
  <action>
In manifest.json, make three additions:

1. host_permissions: add `"https://kick.com/*"` after the youtube entry
2. content_scripts array: add a new entry after the youtube object:
   ```json
   {
     "matches": ["https://kick.com/*"],
     "js": ["content-scripts/kick.js"],
     "css": ["content-scripts/styles.css"],
     "run_at": "document_idle"
   }
   ```
3. web_accessible_resources[0].matches: add `"https://kick.com/*"` to the existing array

Do NOT change any existing manifest entries. The manifest must remain valid JSON.

In webpack.config.js, add one entry to the entry object:
```javascript
'content-scripts/kick': './src/content-scripts/kick.ts',
```
Add it after the youtube entry, before the ui/chat-bundle entry.

After editing, verify manifest.json is valid JSON:
```bash
node -e "JSON.parse(require('fs').readFileSync('manifest.json', 'utf8')); console.log('valid')"
```
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('manifest.json', 'utf8')); console.log('manifest valid')" && grep -c "kick" webpack.config.js</automated>
  </verify>
  <done>manifest.json is valid JSON; contains kick.com in host_permissions, content_scripts, and web_accessible_resources. webpack.config.js contains content-scripts/kick entry.</done>
</task>

<task type="auto">
  <name>Task 2: Build and verify dist/content-scripts/kick.js is produced</name>
  <files></files>
  <action>
Run the build and confirm kick.js appears in the output:
```bash
npm run build
```

After build completes:
1. Confirm `dist/content-scripts/kick.js` exists
2. Confirm the build exits 0 (no errors)
3. Run the postmessage spec stubs to confirm no regression:
   ```bash
   npx playwright test tests/test-postmessage-origin.spec.ts --project=chromium-extension
   ```

If the build fails with TypeScript errors in kick.ts: fix only the errors reported — do not refactor.
If the build fails with manifest errors: validate JSON and re-check the three manifest additions.
  </action>
  <verify>
    <automated>ls dist/content-scripts/kick.js && npx playwright test --project=chromium-extension 2>&1 | tail -8</automated>
  </verify>
  <done>dist/content-scripts/kick.js exists; npx playwright test exits 0 (full suite green, all new Kick stubs still skip). KICK-04 requirement satisfied.</done>
</task>

</tasks>

<verification>
Phase 3 final gate check:

Build output:
```
npm run build 2>&1 | tail -5
```
Exit 0, no errors.

Built artifact:
```
ls dist/content-scripts/kick.js dist/content-scripts/twitch.js dist/content-scripts/youtube.js
```
All three files present.

Full test suite:
```
npx playwright test --project=chromium-extension
```
Exit 0. Existing tests pass. All Kick stubs skip.

Manual step (KICK-03 — required before /gsd:verify-work):
1. Load extension unpacked from dist/ in Chrome
2. Navigate to a live kick.com stream
3. Confirm AllChat iframe replaces native Kick chat
4. Navigate to a second live stream (SPA navigation test)
5. Confirm iframe reinjects in new stream's chat slot
</verification>

<success_criteria>
- manifest.json contains Kick entries in all three required locations (host_permissions, content_scripts, web_accessible_resources)
- webpack.config.js has content-scripts/kick entry
- npm run build succeeds and produces dist/content-scripts/kick.js
- Full Playwright suite exits 0 (no regressions)
- Manual KICK-03 verification performed on live kick.com stream
</success_criteria>

<output>
After completion, create `.planning/phases/03-kick-platform/03-04-SUMMARY.md`
</output>
